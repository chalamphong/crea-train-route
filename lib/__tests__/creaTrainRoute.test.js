const assert = require('assert');
const creaTrainRoute = require('../index.js');

describe('creaTrainRoute', () => {
  it('has a test', () => {
    assert(false, 'creaTrainRoute should have a test');
  });
});
